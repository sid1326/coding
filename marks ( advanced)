#include<stdio.h>
int main()
{
    int grade;
    printf("Enter the grade achieved by student : ");
    scanf("%d",&grade);
    if (grade >= 80) printf("Excellent\n");
    else if (grade >= 60) printf("Not bad\n");
    else printf("Oh...You did not pass\n");
  return 0;
}
